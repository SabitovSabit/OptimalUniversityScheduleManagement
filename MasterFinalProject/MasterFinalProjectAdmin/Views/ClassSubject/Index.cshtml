@model List<ClassSubject>
@{
    ViewData["Title"] = "ClassSubject";
}

<div class="row">
    <div class="col-lg-12 grid-margin stretch-card">
        <div class="card">
            <div class="card-body">
                <h4 class="card-title">Qruplar</h4>
                <a asp-action="Create" class="btn btn-success mt-3">Yeni Qrup Fenn Yarat</a>

                <div class="table-responsive pt-3">
                    <table class="table table-bordered">
                        <thead>
                            <tr>
                                <th>
                                    Qrup Adı
                                </th>
                                  <th>
                                    Fənn Adı
                                </th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (ClassSubject item in Model)
                            {
                                <tr>
                                    <td>
                                        @item.ClassName.Name
                                    </td>
                                     <td>
                                        @item.Subject.Name
                                    </td>
                                    <td>                                      
                                        <a asp-action="Update" asp-controller="ClassSubject" asp-route-id="@item.Id" class="btn btn-warning">
                                            Yenilə
                                            <i class="mdi mdi-table-edit"></i>
                                        </a>
                                        <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger">
                                            Sil
                                            <i class="mdi mdi-delete"></i>
                                        </a>

                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>